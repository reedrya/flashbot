rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore is only accessed through authenticated server routes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
